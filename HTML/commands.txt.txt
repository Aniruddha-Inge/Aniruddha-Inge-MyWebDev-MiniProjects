Steps I followed to puah my project on Github via GIT

git init
git add . 
git commit -m "Written a commit"
git status #just to see working progress


git remote add origin <ssh url.git>
git push origin master